@model FitnessDaw.calendario

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>calendario</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.idCalendario)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.idCalendario)
            @Html.ValidationMessageFor(model => model.idCalendario)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.idUsuario, "usuario")
        </div>
        <div class="editor-field">
            @Html.DropDownList("idUsuario", String.Empty)
            @Html.ValidationMessageFor(model => model.idUsuario)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
